# Copiar estas dependencias al pubspec.yaml del proyecto destino.
# Ajusta versiones si el proyecto destino ya usa otras compatibles.
dependencies:
  cloud_firestore: ^6.5.0
  firebase_auth: ^6.5.2
  firebase_core: ^4.10.0
  flutter_riverpod: ^3.0.3
  go_router: ^17.2.3
